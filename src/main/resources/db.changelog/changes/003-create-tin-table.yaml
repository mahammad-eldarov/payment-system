databaseChangeLog:
  - changeSet:
      id: 003-create-tin-table
      author: Admin
      changes:
        - createTable:
            tableName: tin
            columns:
              - column:
                  name: id
                  type: integer
                  autoIncrement: true
                  constraints:
                    nullable: false
                    primaryKey: true
                    primaryKeyName: tin_pkey

              - column:
                  name: activation_date
                  type: date
                  constraints:
                    nullable: false

              - column:
                  name: balance
                  type: DECIMAL(19,2)
                  defaultValueNumeric: 0
                  constraints:
                    nullable: false

              - column:
                  name: currency
                  type: varchar(3)
                  constraints:
                    nullable: false

              - column:
                  name: customer_id
                  type: integer
                  constraints:
                    nullable: false
                    foreignKeyName: tin_customer_id_fk
                    references: customer(id)

              - column:
                  name: expiry_date
                  type: date
                  constraints:
                    nullable: false

              - column:
                  name: is_visible
                  type: boolean
                  defaultValueBoolean: true
                  constraints:
                    nullable: false

              - column:
                  name: created_at
                  type: timestamp(6)
                  defaultValueComputed: now()

              - column:
                  name: updated_at
                  type: timestamp(6)

              - column:
                  name: tin_number
                  type: varchar(18)
                  constraints:
                    nullable: false

              - column:
                  name: status
                  type: text
                  defaultValue: ACTIVE
                  constraints:
                    nullable: false

              - column:
                  name: tin_holder_name
                  type: varchar
                  constraints:
                    nullable: false

        - sql:
            comment: "tin currency check constraint"
            sql: >
              ALTER TABLE tin
              ADD CONSTRAINT tin_currency_check
              CHECK (currency = ANY (ARRAY['AZN', 'USD', 'EUR']));

        - sql:
            comment: "tin status check constraint"
            sql: >
              ALTER TABLE tin
              ADD CONSTRAINT tin_status_check
              CHECK (status = ANY (ARRAY['ACTIVE', 'SUSPICIOUS', 'BLOCKED', 'CLOSED', 'EXPIRED']));